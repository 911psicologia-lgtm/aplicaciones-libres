# Atlas del Asombro · v29 correcciones

Cambios implementados:

1. Exportar backup JSON: se añadió función global `slug()` para que el nombre del archivo se genere correctamente.
2. Copa final: se eliminó el efecto de caja/halo blanco cuando la copa está bloqueada; ahora conserva fondo oscuro y contorno dorado.
3. Regalo del mapa: se reubicó junto a la copa, sin caja clara, con ícono pulsante y texto titilante.
4. Empaquetado: la app se organizó en carpetas `css/`, `js/`, `assets/` y `docs/`.

Archivo principal: `index.html`.
