════════════════════════════════════════════════════════════════════
  LEEME PRIMERO — Despliegue en Cloudflare (2 minutos)
  Aplicaciones Libres - Audio y Video Super Converter
════════════════════════════════════════════════════════════════════

⚠️  CAUSA Nº1 DE LA "PANTALLA EN BLANCO":
    Subir la carpeta del CÓDIGO FUENTE en vez de esta versión compilada.
    Si al abrir tu página ves blanco (o el panel "No se pudo iniciar la
    aplicación" que menciona /src/main.tsx), es que subiste la carpeta
    equivocada. LA SOLUCIÓN ES ESTE ZIP: contiene la app ya compilada.

────────────────────────────────────────────────────────────────────
CONTENIDO DE ESTA CARPETA (la app ya compilada y lista para subir)
────────────────────────────────────────────────────────────────────
  index.html                  ← la aplicación (primera página)
  favicon.svg                 ← icono de la pestaña
  assets/…                    ← JavaScript + CSS compilados
  ffmpeg/…                    ← motor local (4 fragmentos del núcleo)
  _headers                    ← cabeceras (solo se usa en CF Pages)
  LEEME-DESPLIEGUE.txt        ← este archivo (puedes borrarlo)

  ✅ IMPORTANTE: index.html debe quedar EN LA RAÍZ de lo que subas.
     No lo metas dentro de otra carpeta (p. ej. "HAPPY Audio-video
     super converter"). Si quieres esa ruta, el index.html de la app
     debe estar directamente dentro de ella.

────────────────────────────────────────────────────────────────────
OPCIÓN A — Cloudflare Pages (recomendado, sin comandos)
────────────────────────────────────────────────────────────────────
  1. Entra en https://dash.cloudflare.com → Workers & Pages → Create.
  2. Elige la pestaña "Pages" → "Upload assets" (Subir recursos).
  3. Ponle un nombre al proyecto (p. ej. aplicaciones-libres).
  4. ARRASTRA EL CONTENIDO de este ZIP (o la carpeta descomprimida
     completa: al soltarla, Cloudflare sube su contenido). Debe
     detectar index.html en la raíz.
  5. Pulsa "Deploy site" y abre la URL que te da:
     https://tu-proyecto.pages.dev  ← ¡funcionando!

  Nota: los archivos _headers y el límite de 25 MiB se respetan en
  Pages; el núcleo ya viene partido en 4 fragmentos (<10 MiB cada uno),
  así que no hay ningún archivo grande.

────────────────────────────────────────────────────────────────────
OPCIÓN B — Cloudflare Workers (activos estáticos)
────────────────────────────────────────────────────────────────────
  Requiere Node.js 20+ y ejecutar en la carpeta descomprimida:

      npx wrangler deploy

  con un archivo wrangler.toml (o wrangler.jsonc) junto a index.html:

      name = "aplicaciones-libres"
      compatibility_date = "2025-01-01"

      [assets]
      directory = "./"          # ← la carpeta donde ESTÁ index.html

  Así la app se publica en la raíz:
      https://aplicaciones-libres.<tu-subdominio>.workers.dev/

  ⚠️ En tu despliegue actual la app quedó en
     /HAPPY%20Audio-video-super-converter/ y además era el código
     fuente: por eso se veía en blanco. Con este ZIP y "directory"
     apuntando a la carpeta que contiene index.html, funciona en la raíz.

────────────────────────────────────────────────────────────────────
VERIFICACIÓN RÁPIDA TRAS DESPLEGAR
────────────────────────────────────────────────────────────────────
  1. Abre tu URL → debe verse el tema oscuro esmeralda con el título
     "Aplicaciones Libres - Audio y Video Super Converter".
  2. La pestaña no debe tener favicon roto (favicon.svg está junto a
     index.html).
  3. Arrastra un audio y pulsa "Convertir": el motor pasa de
     "Motor en espera" a "Motor listo" y descargas el resultado.
  Si algo falla, la propia página ahora muestra un panel con el motivo
  exacto y la solución (ya no hay pantalla en blanco silenciosa).

────────────────────────────────────────────────────────────────────
NOVEDAD (auditoría DSEBI): PWA y archivos nuevos imprescindibles
────────────────────────────────────────────────────────────────────
  Esta versión incluye:
    - manifest.webmanifest  → permite "Instalar aplicación" en el
      navegador y el modo sin conexión.
    - sw.js                 → service worker que guarda la app y el
      motor local tras la primera visita.

  ⚠️ Sube TAMBIÉN estos dos archivos junto a index.html (ya vienen en
    el ZIP, no los borres). Si falta sw.js la app funciona igual, pero
  pierde la instalación y el modo offline.

────────────────────────────────────────────────────────────────────
SI LA DESCARGA DEL MOTOR FALLA (novedad)
────────────────────────────────────────────────────────────────────
La aplicación reintenta sola cada fragmento del motor hasta 3 veces y
aborta si el servidor no envía datos en 30 s. Si aun así ves el error,
revisa tu conexión y recarga: los trozos ya descargados quedan en la
caché del navegador (con el service worker activo, normalmente ni se
vuelven a pedir). Un HTTP 404 de un fragmento sigue significando que
falta ffmpeg/ en el despliegue: vuelve a subir dist/ completo.

────────────────────────────────────────────────────────────────────
ENLACES DE YOUTUBE Y OTRAS PLATAFORMAS — CADENA v7
────────────────────────────────────────────────────────────────────
La descarga de enlaces usa una CADENA AUTOADAPTATIVA con salud
persistente (se guarda en tu navegador y aprende de cada intento real):

  1) TUS servidores cobalt (si configuras alguno en Ajustes).
  2) EL MOTOR DE RESCATE (nuevo, funciona hoy mismo):
     · Arranque con CORS verificado → progreso en vivo.
     · Entrega: si el servidor permite leer los bytes, el archivo entra
       en la cola de la app; si no, la app dispara la DESCARGA NATIVA
       del navegador (el archivo llega a tu carpeta «Descargas» con el
       nombre correcto del video — es un límite del servidor, no de la
       app; luego puedes arrastrarlo a la app para convertirlo).
  3) Servidores cobalt públicos de respaldo, ordenados por salud.

SALUD PERSISTENTE (anti-recidivas): cada canal que falla pierde puntos
y queda en «enfriamiento» (no se reintenta hasta que expire); cada uno
que funciona sube puestos. La app se repara sola cuando el ecosistema
cambia — no hace falta actualizar nada.

En Ajustes: «Probar canales ahora» comprueba en vivo el motor de
rescate y tus servidores; campo para tu propia instancia cobalt (con
clave API opcional) si prefieres tu propio servidor.

IMPORTANTE: los servicios de descarga de YouTube cambian sin aviso
(los públicos de código abierto están mayormente rotos desde 2025).
Por eso la cadena ya no depende de ellos: el motor de rescate con
entrega nativa es el canal principal para YouTube.

GRABACIÓN DE ESTUDIO (novedad v5)
El micrófono graba ahora en PCM de estudio: 48 kHz · 16 bits sin
recomprimir (la opción recomendada es «Directo · máxima fidelidad»).
Si vas a grabar en un sitio con ruido, elige «Normal» o «Fuerte» en
Reducción de ruido. No requiere nada especial del servidor: funciona
igual en Cloudflare Pages.

¿ECO EN TUS GRABACIONES? (reforzado en v7)
La casilla «Cancelar el eco de los altavoces» viene ACTIVADA por
defecto y ahora se aplica siempre que la actives, aunque tu navegador
tuviera guardada la preferencia antigua. Elimina el eco que se cuela
cuando algo suena por los altavoces mientras grabas. Si usas
auriculares puedes apagarla para una señal 100 % cruda (la app te
avisará de que con altavoces sonará eco).
